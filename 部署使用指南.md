# Vicky Pocket 部署使用指南

## 离线使用说明

**是的，本应用完全支持离线使用。** 所有数据存储在浏览器的 localStorage 中，不需要网络连接即可正常使用全部功能（记账、图表分析、设置等）。

唯一需要联网的功能：
- **小票识别**：当前为演示模式，如果后续接入 OCR API 则需要联网
- **AI 助手**：当前为本地分析，如果后续接入真实 AI API 则需要联网

---

## 方式一：电脑上使用（推荐）

### 方法 A：直接用浏览器打开（最简单）

1. **构建生产版本**
   ```bash
   cd c:/Users/Nicole/CodeBuddy/20260912133908/app
   npm run build
   ```
   构建产物在 `app/dist/` 文件夹中。

2. **打开应用**
   - 进入 `app/dist/` 文件夹
   - 双击 `index.html` 用浏览器打开即可使用

3. **创建桌面快捷方式**
   - 用 Chrome/Edge 打开 `index.html`
   - 点击浏览器地址栏右侧的"安装"图标（或菜单 → 更多工具 → 创建快捷方式）
   - 勾选"在窗口中打开"，桌面会生成一个应用图标，双击即可像原生应用一样使用

### 方法 B：本地开发服务器（开发调试用）

```bash
cd c:/Users/Nicole/CodeBuddy/20260912133908/app
npm run dev
```
然后在浏览器打开 `http://localhost:5173/`

### 方法 C：打包成桌面应用（可选，需要额外安装）

使用 Tauri 或 Electron 打包成 `.exe` 安装程序：

```bash
# 安装 Tauri CLI
npm install -g @tauri-apps/cli

# 初始化 Tauri
npx tauri init

# 打包
npx tauri build
```
生成的 `.exe` 可直接安装到电脑上，完全离线运行。

---

## 方式二：手机上使用

### 方法 A：部署到免费云端（推荐，手机电脑共享数据）

部署到 Cloudflare Pages、Vercel 或 Netlify 等免费托管平台：

#### 部署到 Vercel

1. 注册 [Vercel](https://vercel.com) 账号
2. 安装 Vercel CLI：`npm i -g vercel`
3. 在项目目录执行：
   ```bash
   cd c:/Users/Nicole/CodeBuddy/20260912133908/app
   vercel
   ```
4. 按提示选择默认配置，部署完成后会获得一个网址如 `https://vicky-pocket.vercel.app`
5. 在手机浏览器打开该网址即可使用
6. **添加到手机桌面**：
   - **iPhone**：Safari 打开 → 分享按钮 → "添加到主屏幕"
   - **Android**：Chrome 打开 → 菜单 → "添加到主屏幕"

> 注意：云端部署后数据存在各设备的 localStorage 中，不同设备间数据不共享。如需跨设备同步，需要接入后端数据库（如 Supabase）。

#### 部署到 Netlify

1. 注册 [Netlify](https://netlify.com)
2. 拖拽 `app/dist/` 文件夹到 Netlify 的部署区域
3. 获得网址后在手机浏览器打开并添加到主屏幕

### 方法 B：手机本地运行（完全离线）

1. 将 `app/dist/` 文件夹中的所有文件传到手机
2. 在手机上用支持打开本地 HTML 的浏览器打开 `index.html`
   - Android：Chrome 可以直接打开 `file://` 路径
   - iPhone：需要用 Files app 或第三方浏览器（如 Documents by Readdle）

### 方法 C：打包成手机 App（可选）

使用 Capacitor 打包成原生 App：

```bash
# 安装 Capacitor
npm install @capacitor/core @capacitor/cli
npx cap init

# 添加平台
npm install @capacitor/android
npm install @capacitor/ios
npx cap add android
npx cap add ios

# 构建并同步
npm run build
npx cap sync

# 打开 Android Studio 构建 APK
npx cap open android
```
生成的 APK 可安装到 Android 手机，完全离线运行。

---

## 方式三：同一局域网共享（电脑当服务器）

1. 在电脑上启动开发服务器并暴露到局域网：
   ```bash
   cd c:/Users/Nicole/CodeBuddy/20260912133908/app
   npm run dev -- --host 0.0.0.0 --port 5173
   ```
2. 查看电脑局域网 IP（如 `192.168.1.100`）
3. 手机和电脑连同一 WiFi，手机浏览器打开 `http://192.168.1.100:5173/`

> 注意：此方式数据在各设备独立存储，不共享。关闭电脑后手机无法访问。

---

## 数据备份与迁移

由于数据存储在 localStorage 中，换设备前可导出备份：

1. 在浏览器控制台执行：
   ```javascript
   // 导出
   const data = {
     expenses: localStorage.getItem('vicky-pocket-expenses'),
     labels: localStorage.getItem('vicky-pocket-labels'),
     theme: localStorage.getItem('vicky-pocket-theme'),
   };
   console.log(JSON.stringify(data, null, 2));
   ```

2. 复制输出的 JSON 文本保存为文件

3. 在新设备上恢复：
   ```javascript
   // 导入（将 data 替换为你的备份内容）
   localStorage.setItem('vicky-pocket-expenses', data.expenses);
   localStorage.setItem('vicky-pocket-labels', data.labels);
   localStorage.setItem('vicky-pocket-theme', data.theme);
   location.reload();
   ```

---

## 快速推荐

| 场景 | 推荐方式 | 难度 | 离线 |
|------|---------|------|------|
| 电脑自用 | 浏览器打开 dist/index.html | 最简单 | ✅ |
| 手机自用 | 部署到 Vercel + 添加到主屏幕 | 简单 | ⚠️ 需联网打开（打开后可离线） |
| 完全离线手机 | 打包 APK (Capacitor) | 中等 | ✅ |
| 电脑+手机共享 | 部署到 Vercel + 接入 Supabase | 中等 | ⚠️ |

> **PWA 提示**：部署到 HTTPS 网址后，应用可以添加 Service Worker 实现 PWA（渐进式 Web 应用），添加到主屏幕后即使断网也能正常打开和使用。
